logging  # Standard library, no need to install
httpx==0.24.1
python-telegram-bot==20.3
apscheduler==3.10.1
asyncio  # Standard library, no need to install
hashlib  # Standard library, no need to install
